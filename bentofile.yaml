service: "wine_service:svc"  # Same as the argument passed to `bentoml serve`
labels:
   owner: steman
   stage: dev
include:
- "*.py"  # A pattern for matching which files to include in the bento
python:
  packages:
  - scikit-learn
  - xgboost
